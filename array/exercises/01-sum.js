/**
 * Task Name: Sum First and Last Array Elements
 * Task description: Write a function that receives an array of strings and prints the sum of first and last element in that array.
 * Expected Result: Input =>['20', '30', '40'] = output => 60
 * Input -> ['10', '17', '22', '33'] = output -> 43
 * Input -> ['11', '58', '69'] = output -> 80
 * 
 * Hint: Use the Number() function
 *  function solve(input) {
 *      let first = Number(input.shift());
 *      let second = Number(input.pop());
 *      console.log(first + second);
 * }
 */