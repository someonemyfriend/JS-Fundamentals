/**
 * Task Name: Day of Week
 * Task description: Write a program which receives a number and prints the
 * corresponding name of the day of week. If the number is NOT a valid day, print 'Invalid day!'.
 * Expected Result:
 * Input => 3 = output -> Wednesday
 * Input -> 6 = output -> Saturday
 * Input -> 11 = output -> Invalid day!
 * 
 * Hint:
 *  function dayOfWeek(day) {
 *      let days = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"];
 *   if (day >= 1 && day <= 7) {
 *      //TODO
 *   } else {
 *      //TODO
 *   }  
 * }
 */